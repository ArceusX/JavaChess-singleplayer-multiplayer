package chess.game;

import java.io.Serializable;

//Color already used as a class name, so creatively choose the British "colour".
public enum Colour implements Serializable {
    BLACK, WHITE, NONE
}